const { userTypes } = require('./userType')
const { projectTypes } = require('./projectType')
const { taskTypes } = require('./taskType')
const { hourlogTypes } = require('./hourlogType')

module.exports = {
  userTypes,
  projectTypes,
  taskTypes,
  hourlogTypes
}
